# F-2 BLOCKED — 현 맵에서 *창발적(emergent)* 정면 교착 강제 불가

**상태:** 시나리오 블로커(코드 아님). 매니저 로직은 건전(유닛테스트로 검증) — 막힌 것은 "현
warehouse.world 에서 두 AMR이 자연스럽게 영구 교착하게 만드는 것".

## 증거 (baseline 3회, 매니저 OFF)
| 시도 | spawn | 결과 | closest approach |
|---|---|---|---|
| 1 (mouths) | amr1(32,12) amr2(32,18) | 둘 다 reroute, goal 미도달 | **2.66m** |
| 2 (tight 3m) | amr1(35.5,13.5) amr2(35.5,16.5) | amr1 핀치서 정지/amr2 reroute | **2.76m** |
| (mid-check) | — | 한쪽 commit, 다른쪽 먼 게이트로 우회 | — |

**핵심: 두 로봇이 R(2m) 내로 접근조차 안 함**(최소 2.66m). 정면 조우·교착이 성립 안 됨.

## 근본 원인 (토폴로지)
- warehouse.world = **완전연결 개방 그리드**: N-S 게이트 6개(X=14.5/21.5/28.5/35.5/42.5/49.5) +
  E-W 주아일 5m 폭. 임의 두 점 사이 경로 다수.
- A* 가 상대 로봇을 코스트맵 장애물로 보고 **수 m 전에 미리 우회 replan** → 절대 가까워지지 않음.
- 막을 수 없음: 로봇 0.4m 폭이라 5m 아일 못 막음. idle amr3/amr4 2대로 대체 게이트 5개 다 못 막음.
  DWA 후진 불가라 핀치에 갇혀도 제자리 회전으로 탈출. 월드 편집은 범위 밖(S-9 2존맵이 그 venue).

## 결론
**창발적 영구 교착은 이 맵에서 불가.** GATE-DETECT/RESOLVE 의 *sim 창발* 검증은
단일 병목 토폴로지(S-9 2존맵)가 있어야 함 — 이번 런 범위 밖.

## 대신 검증한 것 (이번 런에서 전달하는 가치)
- 매니저 탐지/해소/우선순위 **로직 = 유닛테스트로 결정론적 검증**(f2_unit_test.py).
- **음성 테스트**(amr1+보행자, 2번째 AMR 없음) = sim bag 으로 오발동 0 검증(co-location 불요).
- 🔴-1 우선순위 주입 = sim 으로 검증(완료).
- 인프라(launch additive, spawn/obstacle override) = F-1 무회귀 유지.

## 사람 결정 필요 (morning) — 교착 end-to-end demo 방법
1. **(권장) S-9 2존맵 대기**: 단일 병목이 있는 맵에서 창발 교착 자연 발생 → 게이트 그대로 PASS.
2. **controlled demo(현 맵)**: idle 로봇을 핀치에 주차해 강제 co-location → 탐지/해소 시연
   (창발 아님, "엔지니어드"). 부분 가치.
3. **reframe**: "head-on contention" 으로 재정의(영구정지 대신 "비효율 우회 vs 우선순위 직진").
